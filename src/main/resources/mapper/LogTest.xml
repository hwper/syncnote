<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hw.syncnote.dao.LogTestDao">
    <!-- 目的:为DAO接口方法提供sql语句配置-->


    <select id="findAll" resultType="LogTest">
        SELECT id, name, ext1, ext2
        FROM logtest
    </select>






</mapper>